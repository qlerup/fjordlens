# Host port for the web UI
APP_PORT=9080

# Synology paths (default: folders inside /volume1/docker/fjordlens)
PHOTO_DIR=/volume1/docker/fjordlens/Photos
DATA_DIR=/volume1/docker/fjordlens/data

# Note: On first start, the app auto-creates inside DATA_DIR:
# - uploads/originals  (canonical)
# - uploads/converted (canonical)
# - thumbs             (internal cache)
# - converted          (internal cache for viewable copies)
# - tus_uploads        (temporary uploads)

# Flask / app settings
TZ=Europe/Copenhagen
LOG_LEVEL=INFO

# Photoframe feed mode: 0 = real images, 1 = test status card only
PHOTOFRAME_TEXT_ONLY=0

# Optional: source folder used for remote photoframe background updates
# Host path with photoframe files (app/, viewer/, scripts/, config/, systemd/, etc.)
PHOTOFRAME_UPDATE_SOURCE_DIR_HOST=../photoframe
# Container path (normally keep default)
PHOTOFRAME_UPDATE_SOURCE_DIR=/photoframe-update-source
# Max uploaded update zip size in bytes (300 MB default)
PHOTOFRAME_UPDATE_UPLOAD_MAX_BYTES=314572800
